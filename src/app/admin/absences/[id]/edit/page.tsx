import EditAbsence from "@/features/Logs/Pages/EditAbsence/EditAbsence";
import { Suspense } from "react";

const EditPage = () => {
  return (
    <Suspense>
      <EditAbsence />
    </Suspense>
  );
};

export default EditPage;
